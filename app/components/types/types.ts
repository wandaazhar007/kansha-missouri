export type getMenuProps = {
  id: number,
  name: string,
  slug: string,
  desc: string,
  price: number,
  urlImage: string
}

export type openModalTypes = {
  openModal: boolean,
  closeModal: any,
  propId: number
}